{
  "name": "AstraasWebPatcher",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "express": "^4.17.1",
    "puppeteer": "^23.3.0"
  },
  "engines": {
    "node": ">=14.x"
  }
  "scripts": {
  "build": "npx puppeteer install && your-build-command"
}
}
